<span class="brand-prompt" aria-hidden="true">&gt;</span>
<span class="brand-name"><span>cmd</span>impact</span>

<style>
	.brand-prompt {
		display: grid;
		width: 2rem;
		height: 2rem;
		place-items: center;
		border-radius: 0.55rem;
		background: var(--accent);
		color: #17120d;
		font: 850 1rem/1 var(--mono);
	}

	.brand-name {
		color: var(--ink);
		font-size: 1.05rem;
		font-weight: 850;
		letter-spacing: -0.045em;
	}

	.brand-name span {
		font-family: var(--mono);
		font-weight: 700;
	}
</style>
