<span class="brand-mark" aria-hidden="true">
	<svg viewBox="0 0 40 40" role="img">
		<path class="mark-ring" d="M20 3.5a16.5 16.5 0 1 0 0 33 16.5 16.5 0 0 0 0-33Z" />
		<path class="mark-route" d="m12.2 21.1 5.1 5.1 10.6-12.4" />
		<path class="mark-north" d="M27.9 13.8v6.5" />
	</svg>
</span>
<span class="brand-name">Compat<span>Navi</span></span>

<style>
	.brand-mark {
		display: grid;
		width: 2.15rem;
		height: 2.15rem;
		place-items: center;
		color: var(--accent);
	}

	.brand-mark svg {
		width: 100%;
		height: 100%;
		fill: none;
		stroke: currentColor;
		stroke-linecap: round;
		stroke-linejoin: round;
	}

	.mark-ring {
		stroke-width: 2.4;
		opacity: 0.28;
	}

	.mark-route {
		stroke-width: 3.2;
	}

	.mark-north {
		stroke: var(--teal);
		stroke-width: 3.2;
	}

	.brand-name {
		color: var(--ink);
		font-size: 1.18rem;
		font-weight: 780;
		letter-spacing: -0.045em;
	}

	.brand-name span {
		color: var(--accent);
	}
</style>
