<span class="brand-mark" aria-hidden="true">
	<svg viewBox="0 0 44 44">
		<rect class="mark-frame" x="3" y="3" width="38" height="38" rx="12" />
		<path class="mark-link" d="M12 22h7m6 0h7M22 12v7m0 6v7" />
		<rect class="mark-core" x="18" y="18" width="8" height="8" rx="2" />
		<circle cx="12" cy="22" r="2" /><circle cx="32" cy="22" r="2" /><circle cx="22" cy="12" r="2" /><circle cx="22" cy="32" r="2" />
	</svg>
</span>
<span class="brand-name">Compat<span>Navi</span><small>GLOBAL</small></span>

<style>
	.brand-mark { display: grid; width: 2.35rem; height: 2.35rem; flex: 0 0 auto; place-items: center; color: var(--signal); }
	.brand-mark svg { width: 100%; height: 100%; overflow: visible; fill: none; stroke: currentColor; stroke-linecap: round; stroke-linejoin: round; }
	.mark-frame { fill: color-mix(in srgb, var(--signal) 10%, transparent); stroke: color-mix(in srgb, var(--signal) 42%, transparent); }
	.mark-link { stroke-width: 1.6; }
	.mark-core { fill: var(--signal); stroke: none; }
	.brand-mark circle { fill: var(--canvas); stroke-width: 1.7; }
	.brand-name { display: inline-flex; align-items: baseline; color: var(--ink); font-size: 1.08rem; font-weight: 800; letter-spacing: -0.045em; line-height: 1; }
	.brand-name > span { color: var(--signal); }
	.brand-name small { margin-left: 0.48rem; color: var(--faint); font-size: 0.5rem; font-weight: 800; letter-spacing: 0.14em; }
</style>
